# ClawArmor

Security armor for OpenClaw agents.

```bash
npx clawarmor audit    # score your config
npx clawarmor scan     # scan installed skills
npx clawarmor monitor  # continuous monitoring (paid)
```

clawarmor.dev
